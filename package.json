{
  "name": "groove",
  "description": "Copy component tree paths from any page (React, Vue, Angular, Svelte)",
  "private": true,
  "version": "1.0.5",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "postbuild": "npm version patch --no-git-tag-version",
    "zip": "wxt zip",
    "postzip": "npm version patch --no-git-tag-version",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@wxt-dev/module-react": "^1.0.0",
    "puppeteer": "^24.37.3",
    "typescript": "^5.0.0",
    "wxt": "^0.20.0"
  }
}
